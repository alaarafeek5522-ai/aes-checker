# aes-checker

AES-based SSL bypass and cookie challenge solver.

## Install

```bash
pip install aes-checker
```

## Usage

```python
from aes_checker import check

result = check("https://example.com")
print(result)
```
